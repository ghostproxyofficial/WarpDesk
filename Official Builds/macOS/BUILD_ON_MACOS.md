# Build WarpDesk on macOS

From repository root:

```bash
cd agent/tauri-control
npm install
npm run tauri build
```

Expected output (example):

- src-tauri/target/release/bundle/dmg/*.dmg

Copy artifacts into this folder.
